__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.env
.context.yaml

# User-specific config (contains secrets) — use *.example.* as templates
config.yaml

# Cursor IDE — ignore everything except example config and custom rules
.cursor/*
!.cursor/mcp.example.json
!.cursor/rules/
.cursor/rules/*
!.cursor/rules/mcp10x.mdc
